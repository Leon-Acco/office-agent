# Web 框架(fastapi 0.115.12+ 才正式兼容 pydantic 2.11)
fastapi==0.115.14
uvicorn[standard]==0.31.1
python-multipart==0.0.20

# 数据校验(pydantic>=2.11 / pydantic-settings>=2.5.2 是 mcp 1.26.0 的硬性要求)
pydantic==2.11.10
pydantic-settings==2.5.2

# MySQL（异步驱动）
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
pymysql==1.1.1

# Redis
redis[hiredis]==5.2.0

# HTTP 客户端（调用 LLM API）;>=0.27.1 是 mcp 1.26.0 的硬性要求
httpx==0.27.2

# YAML 解析（岗位包配置）
pyyaml==6.0.2

# MCP 客户端（官方 SDK，SSE transport 支持自定义 headers）
mcp==1.26.0
